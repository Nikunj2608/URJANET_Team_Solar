# Copy this file to .env and fill in the real secret values.
# Never commit a real API key.

# Google Gemini API (place real key ONLY in .env, never commit it)
GEMINI_API_KEY=REPLACE_WITH_REAL_GEMINI_KEY

# (Optional) Override model (defaults set in docker-compose)
# GEMINI_MODEL=gemini-1.5-flash

# --- AI Endpoint Protection (optional) ---
# Static token clients must send in header x-ai-key or query ?key=
AI_ACCESS_TOKEN=REPLACE_AI_ENDPOINT_TOKEN
# Requests allowed per minute per token/IP (blank or 0 disables throttling)
AI_RATE_LIMIT_PER_MIN=30
